#include <cstdio>
#include <cstring>
#include <vector>
#include <string>
using namespace std;

int main(){
	scanf("%d\n",&tc);
	while (tc--){
		orang.clear();
		memset(package,0,sizeof(package));
		np = 0;
		
		getline(cin,ss);
		scanf("%d\n",&n);
		for (int k=0; k<n; ++i){
			while (sscanf(ss,"%s",&s)) add(s), orang.push_back(s);
			for (int i=1; i<orang.size(); ++i) ++package[hash[orang[i]]];
		}
		
		ans = 0;
		for (int k=0; k<
			
