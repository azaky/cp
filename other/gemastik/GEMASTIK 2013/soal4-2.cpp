#include <stdio.h>
#include <vector>
#include <algorithm>
using namespace std;

struct dikra{
    id, order, jenis;
};

int main(){
    scanf("%d",&n);
    vector <dikra> anjing, kucing;
    for (int i=0; i < n; ++i){
	dikra a;
	scanf("%d%d%d",&a.id,&a.order,&a.jenis);
	if (id == 0) anjing.push_back(a); else kucing.push_back(b);
    }
    sort
