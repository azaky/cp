#include <stdio.h>
#include <stdlib.h>

main(){
       int n;
       scanf("%d",&n);
       for (int i=0; i<n; i++)
           printf("Hello World!\nHalo Dunia!\n");
       system ("pause");
       exit(0);
       }
