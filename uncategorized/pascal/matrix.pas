var

begin
  readln(n,m,x);
  for i:=1 to n do
    for j:=1 to m do
      if j=m then readln(a[i,j]) else read(a[i,j]);
  for i:=1 to x do
    begin
      readln(task);
      if
    end;
end.