program RealGrader;

uses
  //testcase,
  unix;

var
  CurDir:ansistring;

begin
  //Get the Current Directory
  GetDir(0,CurDir);
  {
  //read the data
  readln(Prog);
  readln(FName);
  
  //copying the files
  Dir:=CurDir+'
  }
  fpsystem('fpc timer.pas > '+CurDir+'/output.txt');
  {
  //run the program
  
  //check the result
  }
end.

